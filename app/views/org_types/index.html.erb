<h1>Listing org_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @org_types.each do |org_type| %>
  <tr>
    <td><%= org_type.name %></td>
    <td><%= org_type.description %></td>
    <td><%= link_to 'Show', org_type %></td>
    <td><%= link_to 'Edit', edit_org_type_path(org_type) %></td>
    <td><%= link_to 'Destroy', org_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Org type', new_org_type_path %>
